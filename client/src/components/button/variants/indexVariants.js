import { variantColors } from "./colorVariants";
import { sizeStyles } from "./sizeVariants";
import { shapeStyles } from "./shapeVariants";
import { stateStyles } from "./stateVariants";
import { shadowStyles } from "./shadowVariants";
import { textStyleVariants } from "./textVariants";
import { animationVariants } from "./animationVariants";

const defaultVariants = {
  variantColors,
  sizeStyles,
  shapeStyles,
  stateStyles,
  shadowStyles,
  textStyleVariants,
  animationVariants,
};

export default defaultVariants;
